# CLAUDE.md

Project managed with aikuora-dev. Artifacts are in `.aikuora/`.

- `/specify` — Define or refine the project spec
- `/plan` — Generate implementation plan from spec.yaml
- `/status` — Quick project status
- `/init` — Initialize .aikuora/ structure
- `/migrate` — Onboard an existing project

@.aikuora/codebase.md
