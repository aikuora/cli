name: tailwind
description: "Tailwind CSS utility framework configuration"
kind: shareable
lang: typescript
requires:
  - pnpm

workspace:
  vscode:
    extensions:
      - "bradlc.vscode-tailwindcss"
    settings:
      css.lint.unknownAtRules: ignore

link:
  dependency: true
  targetFile: postcss.config.mjs
  content: |
    export default {
      plugins: {
        '@tailwindcss/postcss': {},
      },
    };
